# Works with build image 14.04 (Trusty)

machine:
    environment:
        DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test

dependencies:
  override:
    - pyenv local 3.5.9 3.7.6
    - pip install tox tox-pyenv detox

test:
  override:
    - tox
